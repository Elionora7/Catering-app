/** Minimum cart subtotal (before delivery) required to open checkout */
export const MIN_CHECKOUT_SUBTOTAL = 150

/** Stored on `delivery_zones.minimumOrder` — enforced for delivery in API + seed */
export const MIN_DELIVERY_ZONE_ORDER = 150
